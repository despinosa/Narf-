<Page x:Class="Narf_.view.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Narf_"
      xmlns:emgu="clr-namespace:Emgu.CV.UI;assembly=Emgu.CV.UI"
      mc:Ignorable="d" 
      d:DesignHeight="540" d:DesignWidth="720"
      Title="HomePage">

    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="430" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="240"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- People list -->
        <Label Grid.Column="0" VerticalAlignment="Center" FontFamily="Trebuchet MS" 
        FontWeight="Bold" FontSize="18" Foreground="#0066cc" Content="Análisis de video"/>
        <Border Grid.Column="0" Grid.Row="1" Height="35" Padding="5" Background="#4E87D4">
            <Label VerticalAlignment="Center" Foreground="White">Casos previos</Label>
        </Border>
        <!--
        <ListBox Name="peopleListBox" Grid.Column="0" Grid.Row="2">
            <ListBoxItem>xMaze_11feb16</ListBoxItem>
            <ListBoxItem>xMaze_11feb16</ListBoxItem>
            <ListBoxItem>noMaze_11feb16</ListBoxItem>
            <ListBoxItem>xMaze_11feb16</ListBoxItem>
        </ListBox>
        -->
        <!-- Analyze new button -->
        <Button Name="newBttn" Grid.Column="0" Grid.Row="3" Margin="0,10,0,0" Width="125"
Height="25" HorizontalAlignment="Left">Nuevo...</Button>
        
        <!-- Preview -->
        <WindowsFormsHost Grid.Column="1" Grid.Row="2" Margin="10, 0, 0, 0" Width="420" Height="240">
            <emgu:ImageBox x:Name="previewDisplay" LoadCompleted="previewDisplay_LoadCompleted"/>
        </WindowsFormsHost>

        <!-- View report button -->
        <Button Name="resultsBttn" Grid.Column="1" Grid.Row="3" Margin="0,10,0,0" Width="125"
Height="25" HorizontalAlignment="Right">Resultados</Button>
    </Grid>
    <Page.Background>
        <ImageBrush ImageSource="/resources/background.jpg"/>
    </Page.Background>
</Page>
